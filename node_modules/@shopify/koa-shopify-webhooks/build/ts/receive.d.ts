import type { Context, Middleware } from 'koa';
import type { Topic } from './types';
interface Options {
    secret: string;
    path?: string;
    onReceived?(ctx: Context): any;
}
export interface WebhookState {
    topic: Topic;
    domain: string;
}
export declare function receiveWebhook({ secret, path, onReceived, }: Options): Middleware;
export {};
//# sourceMappingURL=receive.d.ts.map